import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'in.crystalcubes.coldstorage',
  appName: 'Cold Storage',
  webDir: 'dist'
};

export default config;
