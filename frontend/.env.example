# Base URL for API requests. Keep empty for standard web builds (resolves relatively).
# Set to the absolute API origin (e.g. https://cold.crystalcubes.in) for packaged mobile (Capacitor) builds.
VITE_API_BASE_URL=
